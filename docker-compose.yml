version: '3.8'

services:
  app-amor2:
    image: app-amor-2
    build: .
    ports:
      - "3003:3003"  # Cambié el puerto 3000 a 3002 en el host
    networks:
      - red-external-network
    restart: always

networks:
  red-external-network:
    external: true
